<form method="post"
      action="{{ path('app_meuble_delete', {'id': meuble.id}) }}"
      onsubmit="return confirm('Voulez-vous vraiment supprimer ce meuble ?');">

    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ meuble.id) }}">

    <button class="btn btn-danger">
        Supprimer
    </button>
</form>